{
  "name": "warranty-vault",
  "version": "1.0.0",
  "private": true,
  "description": "Track every warranty you own, get warned before coverage expires, and generate claim letters when something breaks.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
